var cantidad = prompt("Ingrese la cantidad de veces quiere el saludo:");
var centinela = 0;

// Procesamiento
while (centinela < cantidad) {
  alert(`Hola que tal ${centinela+1}`);
  // 
  centinela++
}


